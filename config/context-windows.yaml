# ---
# title: "Context window sizes — model → advertised tokens map"
# created_at: 2026-05-10--20-45
# created_by: OpenCode (Claude Opus 4.7 1M, via /brain actor)
# updated_by: Actor (claude-haiku-4-5 via /brain)
# updated_at: 2026-09-18--22-25
# context: >
#   Used by scripts/ctx-segment.sh as the denominator for the status-line
#   ctx fill segment. Lookup is normalised + [1m]-aware. Fallback when a
#   model_id isn't here: OC's own context_window.context_window_size.
# ---
# context-windows.yaml — advertised context window sizes per model ID
# Used by scripts/ctx-segment.sh for the status-line ctx denominator.
# Fallback: OC's own context_window.context_window_size if model_id absent.
#
# Model ID normalisation: strip trailing [1m], [200k], -YYYYMMDD patterns
# before lookup. If original (unstripped) ID contains [1m], use 1000000
# regardless of map lookup — displayed as 1.0M or 1M depending on values.
#
# last_updated: YYYY-MM-DD
updated_at: 2026-09-18--22-25
updated_by: Actor (claude-haiku-4-5 via /brain)
last_updated: 2026-09-18
models:
  claude-opus-5:               1000000
  claude-opus-5[1m]:           1000000
  claude-sonnet-5:             200000
  claude-sonnet-5[1m]:         1000000
  claude-fable-5:              1000000
  claude-fable-5[1m]:          1000000
  claude-fable-5-1:     1000000
  claude-fable-5-1[1m]: 1000000
  claude-opus-4-7:            1000000
  claude-opus-4-7[1m]:        1000000
  claude-sonnet-4-6:          200000
  claude-sonnet-4-5:          200000
  claude-haiku-4-5:           200000
  # SoHoAI-routed models — canonical OC IDs (sohoai/<key> per opencode.json).
  # Values sourced from SoHoAI config.yaml model_info.context_window
  # (advertised via /proxy/v1/model/info to clients).
  sohoai/minimax-m3:      1000000  # Ollama Cloud; 1M context
  sohoai/deepseek-v4-pro: 1000000  # Ollama Cloud; 1M context
  sohoai/glm-5.3:         1000000  # Ollama Cloud; 1M context
  sohoai/deepseek-v4.1-flash: 1000000
  sohoai/glm-5.3-flash:       1000000
  sohoai/qwen3.5-9b-q4:        131072
  sohoai/claude-haiku-4-5:     200000
  sohoai/kimi-k3:             1000000   # HTTP 402 at Ollama Cloud; falls back to PAID claude-sonnet-5
