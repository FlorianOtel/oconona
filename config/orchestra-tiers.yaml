# ---
# title: "Tier-to-model assignment — SSOT for Oracle subagent tier selection"
# created_at: 2026-06-03--07-52
# created_by: Claude Code (Haiku 4.5 via Step 1-3 Actor dispatch)
# context: >
#   Central configuration for Brain's subagent tier → model dispatch policy.
#   Used by scripts/check-tiers.py to verify agent frontmatter, model-rates.yaml,
#   and context-windows.yaml at deploy time for drift detection.
#   Do NOT change model values here without also updating agents/*.md frontmatter.
# ---

# orchestra-tiers.yaml — SSOT for tier → model assignment.
# Used by scripts/check-tiers.py to verify agent frontmatter, model-rates.yaml,
# and context-windows.yaml at deploy time. Do NOT change model values here
# without also updating the relevant agents/*.md frontmatter.

tiers:
  planner:     { model: sohoai/minimax-m3 }
  actor:       { model: sohoai/qwen3-4b-q6 }
  actor-heavy: { model: sohoai/glm-5.1 }
  reviewer:    { model: anthropic/claude-sonnet-4-6 }
  researcher:      { model: anthropic/claude-haiku-4-5 }
  researcher-deep: { model: anthropic/claude-sonnet-4-6 }

recommendations:
  brain: { model: anthropic/claude-opus-4-7, enforcement: advisory }
  duo:   { model: anthropic/claude-sonnet-4-6, enforcement: advisory }
